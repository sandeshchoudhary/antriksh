# Antrisk
Open Library

- Build docker image
```sh
$ git clone https://github.com/sandeshchoudhary/antriksh.git
$ cd antriksh
$ docker-compose build --force
```

- Run docker image
```sh
$ docker-compose -up
```
